diff --git a/trade_rl/rl/lagrangian_probe.py b/trade_rl/rl/lagrangian_probe.py
--- a/trade_rl/rl/lagrangian_probe.py
+++ b/trade_rl/rl/lagrangian_probe.py
@@ -268,11 +268,13 @@ def run_canonical_action_feasibility_probe(
                 if canonical_semantic is None:
                     canonical_semantic = semantic
                     canonical_action = action.copy()
-                elif semantic is not canonical_semantic or not np.array_equal(
-                    action,
-                    canonical_action,
-                ):
-                    raise ValueError("canonical probe environment contract changed")
+                else:
+                    if canonical_action is None:
+                        raise RuntimeError("canonical probe action state is inconsistent")
+                    if semantic is not canonical_semantic or not np.array_equal(
+                        action,
+                        canonical_action,
+                    ):
+                        raise ValueError("canonical probe environment contract changed")
 
                 environment.reset(seed=completed_count)
