name: Apply causal training availability temporary

on:
  push:
    branches:
      - feature/universal-causal-alpha-teacher

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: feature/universal-causal-alpha-teacher
          fetch-depth: 0
      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86
        with:
          python-version: '3.12'
      - name: Apply availability-aware fit
        run: python tools/_temporary_apply_causal_training_availability.py
      - name: Verify availability-aware fit
        run: |
          set -euo pipefail
          uv sync --extra dev --extra train-sb3
          files=(
            trade_rl/learning/causal_alpha_teacher.py
            trade_rl/workflows/universal_stage_a_training.py
            tests/workflows/test_universal_causal_alpha_shared_training.py
            tests/learning/test_causal_alpha_teacher_training_availability.py
          )
          uv run ruff check --fix --select I "${files[@]}"
          uv run ruff format "${files[@]}"
          uv run ruff check "${files[@]}"
          uv run ruff format --check "${files[@]}"
          uv run mypy trade_rl/learning/causal_alpha_teacher.py
          uv run pytest \
            tests/learning/test_causal_alpha_teacher.py \
            tests/learning/test_causal_alpha_teacher_prediction_availability.py \
            tests/learning/test_causal_alpha_teacher_training_availability.py \
            tests/workflows/test_universal_causal_alpha_fitting.py \
            tests/workflows/test_universal_causal_alpha_selection.py \
            tests/workflows/test_universal_causal_alpha_package.py \
            tests/workflows/test_universal_causal_alpha_assembly.py \
            tests/workflows/test_universal_causal_alpha_shared_training.py \
            -q
      - name: Commit verified fit and remove temporary assets
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm -f \
            tools/_temporary_apply_causal_training_availability.py \
            .github/workflows/apply-causal-training-availability-temporary.yml \
            .github/workflows/causal-alpha-training-availability-red-temporary.yml
          git add \
            trade_rl/learning/causal_alpha_teacher.py \
            trade_rl/workflows/universal_stage_a_training.py \
            tests/workflows/test_universal_causal_alpha_shared_training.py \
            tests/learning/test_causal_alpha_teacher_training_availability.py
          git commit -m 'fix: fit causal ridge with feature availability'
          git push origin HEAD:feature/universal-causal-alpha-teacher
