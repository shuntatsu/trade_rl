name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Apply causal hardening
    runs-on: ubuntu-latest
    steps:
      - name: Checkout head branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Apply patch and restore normal CI
        run: |
          cat .github/hardening-patch/part*.patch > /tmp/causal-hardening.patch
          git apply --check /tmp/causal-hardening.patch
          git apply /tmp/causal-hardening.patch
          git show 5f4dee28df003e6e132e91f1d2840315e606848a:.github/workflows/ci.yml > .github/workflows/ci.yml
          rm -rf .github/hardening-patch .github/hardening-payload
          rm -f .github/workflows/apply-causal-hardening.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: close final causal data contract gaps"
          git push origin HEAD:${{ github.head_ref }}
