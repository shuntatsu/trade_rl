name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Rebuilt Core
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
          enable-cache: true

      - name: Install
        run: uv sync --extra dev >/dev/null

      - name: Ruff
        run: uv run ruff check .

      - name: Capture full format diagnostics
        id: full-format
        shell: bash
        run: |
          set +e
          uv run ruff format --check --diff . > full-format.diff 2>&1
          status=$?
          echo "status=$status" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload full format diagnostics
        uses: actions/upload-artifact@v4
        with:
          name: full-format-diagnostics
          path: full-format.diff
          if-no-files-found: error
          retention-days: 1

      - name: Fail on format differences
        if: steps.full-format.outputs.status != '0'
        run: exit 1
