name: Diagnose Binance metadata task 3 MyPy

on:
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/diagnose-binance-metadata-task3-mypy.yml'

permissions:
  contents: read

jobs:
  mypy-diagnostics:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/binance-metadata-modes-task3
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v5
      - name: Apply locally verified patch
        shell: bash
        run: |
          set -euo pipefail
          encoded=$(sed -n 's/^          PATCH_GZIP_BASE64: //p' .github/workflows/apply-binance-metadata-task3.yml)
          test -n "$encoded"
          printf '%s' "$encoded" | base64 -d | gzip -d > /tmp/task3.patch
          git apply --check /tmp/task3.patch
          git apply /tmp/task3.patch
      - run: uv sync --extra dev --extra train-sb3
      - run: |
          uv run ruff check --fix .
          uv run ruff format .
      - name: Capture MyPy
        shell: bash
        run: |
          set +e
          uv run mypy trade_rl > /tmp/mypy.log 2>&1
          echo "$?" > /tmp/mypy.status
          set -e
          cat /tmp/mypy.log
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: metadata-task3-mypy
          path: |
            /tmp/mypy.log
            /tmp/mypy.status
          if-no-files-found: error
