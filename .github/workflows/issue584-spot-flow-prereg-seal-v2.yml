name: Issue 584 Spot-flow continuation preregistration seal v2

on:
  push:
    branches:
      - seal/issue584-spot-flow-prereg-v2
    paths:
      - .github/workflows/issue584-spot-flow-prereg-seal-v2.yml

permissions:
  actions: read
  contents: read

env:
  TARGET_SHA: 89ec1092e438d69657840567e684739ca0c4e3d5
  FULL_VERIFY_RUN_ID: "34932104475"
  FULL_VERIFY_WORKFLOW: .github/workflows/tmp-issue584-prereg-full-89ec.yml
  DIGEST_VERIFY_RUN_ID: "34932286179"
  SUPERSEDED_SEAL_RUN_ID: "34931962963"
  PROTOCOL_DIGEST: bf0aa2db248e9dcab745e92e6bde54de6b48276a785eec74670e0dedba8cb5e3
  SOURCE_PATH: trade_rl/evaluation/experiments/bootstrap/spot_flow_continuation_prereg.py
  TEST_PATH: tests/evaluation/experiments/bootstrap/test_spot_flow_continuation_prereg.py
  HARDENING_TEST_PATH: tests/evaluation/experiments/bootstrap/test_spot_flow_continuation_prereg_hardening.py
  TARGET_SOURCE_TEST_PATH: tests/evaluation/experiments/bootstrap/test_spot_flow_continuation_prereg_target_source.py

jobs:
  seal:
    name: Immutable final protocol publisher
    runs-on: ubuntu-24.04
    outputs:
      artifact_name: ${{ steps.meta.outputs.name }}
      artifact_id: ${{ steps.upload.outputs.artifact-id }}
      artifact_digest: ${{ steps.upload.outputs.artifact-digest }}
    steps:
      - name: Checkout exact final preregistration authority
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ env.TARGET_SHA }}
          persist-credentials: false
      - name: Bind final head, verification, digest, and superseded v1
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$TARGET_SHA"
          test "$(git ls-remote origin refs/heads/research/issue584-spot-flow-prereg | cut -f1)" = "$TARGET_SHA"

          full="$(gh api repos/$GITHUB_REPOSITORY/actions/runs/$FULL_VERIFY_RUN_ID)"
          test "$(jq -r .conclusion <<<"$full")" = success
          test "$(jq -r .path <<<"$full")" = "$FULL_VERIFY_WORKFLOW"
          full_jobs="$(gh api repos/$GITHUB_REPOSITORY/actions/runs/$FULL_VERIFY_RUN_ID/jobs?per_page=100)"
          test "$(jq -r '.jobs[] | select(.name=="Lean Core exact target") | .conclusion' <<<"$full_jobs")" = success
          test "$(jq -r '.jobs[] | select(.name=="Human Guide exact target") | .conclusion' <<<"$full_jobs")" = success

          digest_run="$(gh api repos/$GITHUB_REPOSITORY/actions/runs/$DIGEST_VERIFY_RUN_ID)"
          test "$(jq -r .conclusion <<<"$digest_run")" = success
          digest_jobs="$(gh api repos/$GITHUB_REPOSITORY/actions/runs/$DIGEST_VERIFY_RUN_ID/jobs?per_page=100)"
          test "$(jq -r '.jobs[] | select(.name=="digest") | .conclusion' <<<"$digest_jobs")" = success

          old="$(gh api repos/$GITHUB_REPOSITORY/actions/runs/$SUPERSEDED_SEAL_RUN_ID)"
          test "$(jq -r .conclusion <<<"$old")" = failure
          old_artifacts="$(gh api repos/$GITHUB_REPOSITORY/actions/runs/$SUPERSEDED_SEAL_RUN_ID/artifacts?per_page=100)"
          test "$(jq -r .total_count <<<"$old_artifacts")" = "0"
      - name: Setup Python
        uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
        with:
          version: "0.10.0"
          python-version: "3.12"
      - name: Install
        run: uv sync --extra dev
      - name: Reprove final preregistration contract
        shell: bash
        run: |
          set -euo pipefail
          files="$SOURCE_PATH $TEST_PATH $HARDENING_TEST_PATH $TARGET_SOURCE_TEST_PATH"
          uv run ruff check $files
          uv run ruff format --check $files
          uv run mypy "$SOURCE_PATH"
          uv run pytest -q "$TEST_PATH" "$HARDENING_TEST_PATH" "$TARGET_SOURCE_TEST_PATH"
      - name: Materialize canonical protocol and source-test blob authority
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/seal
          uv run python - <<'PY'
          import hashlib
          import os
          import subprocess
          from pathlib import Path
          from trade_rl.artifacts.canonical import canonical_json_bytes
          from trade_rl.evaluation.experiments.bootstrap.spot_flow_continuation_prereg import canonical_spot_flow_continuation_protocol, load_spot_flow_continuation_protocol_bytes

          protocol = canonical_spot_flow_continuation_protocol()
          raw = protocol.canonical_json_bytes
          expected = os.environ['PROTOCOL_DIGEST']
          if protocol.digest != expected or hashlib.sha256(raw).hexdigest() != expected:
              raise SystemExit('protocol digest/bytes authority differs')
          if load_spot_flow_continuation_protocol_bytes(raw).digest != expected:
              raise SystemExit('strict protocol reload differs')
          payload = protocol.to_payload()
          if payload.get('target_source_family') != 'binance_vision_contract_klines' or payload.get('target_transport_mode') != 'VISION':
              raise SystemExit('final target source authority differs')
          for key in ('target_source_fallback_allowed','target_source_replacement_allowed','economic_values_inspected','target_relation_computed','training_relation_executed','evaluation_pnl_inspected','evaluation_execution_authorized','final_test_authorized','shared_cash_profitability_established','production_eligible','live_trading_authorized','alternate_sign_allowed','alternate_window_allowed','alternate_horizon_allowed','alternate_weighting_allowed','symbol_subset_allowed','additional_spot_dates_allowed'):
              if payload.get(key) is not False:
                  raise SystemExit(f'forbidden preregistration flag differs: {key}')
          for key in ('target_source_checksum_required','target_source_manifest_binding_required','target_source_structural_preflight_required'):
              if payload.get(key) is not True:
                  raise SystemExit(f'required target source flag differs: {key}')
          if payload.get('spot_source_status') != 'PASS_SPOT_AGGTRADES_SOURCE' or payload.get('spot_source_usable_rows') != 13_598_332:
              raise SystemExit('Spot source authority differs')

          paths = [os.environ['SOURCE_PATH'], os.environ['TEST_PATH'], os.environ['HARDENING_TEST_PATH'], os.environ['TARGET_SOURCE_TEST_PATH']]
          blobs = {path: subprocess.check_output(['git','rev-parse',f'HEAD:{path}'], text=True).strip() for path in paths}
          Path('/tmp/seal/protocol.json').write_bytes(raw)
          index = {
              'schema_version': 'issue584_spot_flow_prereg_seal_v2',
              'issue_number': 584,
              'target_sha': os.environ['TARGET_SHA'],
              'full_verify_run_id': int(os.environ['FULL_VERIFY_RUN_ID']),
              'digest_verify_run_id': int(os.environ['DIGEST_VERIFY_RUN_ID']),
              'superseded_seal_run_id': int(os.environ['SUPERSEDED_SEAL_RUN_ID']),
              'protocol_digest': expected,
              'protocol_json_sha256': expected,
              'blob_shas': blobs,
              'spot_source_status': 'PASS_SPOT_AGGTRADES_SOURCE',
              'spot_source_usable_rows': 13_598_332,
              'target_source_family': 'binance_vision_contract_klines',
              'target_transport_mode': 'VISION',
              'economic_values_inspected': False,
              'target_relation_computed': False,
              'training_relation_executed': False,
              'evaluation_pnl_inspected': False,
              'production_eligible': False,
              'live_trading_authorized': False,
          }
          Path('/tmp/seal/seal-index.json').write_bytes(canonical_json_bytes(index))
          print('PROTOCOL_DIGEST=' + expected)
          print('TARGET_SOURCE_FAMILY=binance_vision_contract_klines')
          print('ECONOMIC_VALUES_INSPECTED=false')
          print('TARGET_RELATION_COMPUTED=false')
          PY
      - id: meta
        run: echo "name=issue584-spot-flow-prereg-seal-v2-${GITHUB_RUN_ID}" >> "$GITHUB_OUTPUT"
      - name: Upload immutable final seal artifact
        id: upload
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: ${{ steps.meta.outputs.name }}
          path: /tmp/seal/
          if-no-files-found: error
          compression-level: 0
          retention-days: 90

  fresh:
    name: Fresh independent final protocol reconstruction
    needs: seal
    runs-on: ubuntu-24.04
    outputs:
      artifact_name: ${{ steps.meta.outputs.name }}
      artifact_id: ${{ steps.upload.outputs.artifact-id }}
      artifact_digest: ${{ steps.upload.outputs.artifact-digest }}
    steps:
      - name: Checkout exact final authority independently
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ env.TARGET_SHA }}
          persist-credentials: false
      - name: API-bind seal artifact
        env:
          GH_TOKEN: ${{ github.token }}
          ARTIFACT_ID: ${{ needs.seal.outputs.artifact_id }}
          ARTIFACT_NAME: ${{ needs.seal.outputs.artifact_name }}
          ARTIFACT_DIGEST: ${{ needs.seal.outputs.artifact_digest }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$TARGET_SHA"
          test "$(git ls-remote origin refs/heads/research/issue584-spot-flow-prereg | cut -f1)" = "$TARGET_SHA"
          payload="$(gh api repos/$GITHUB_REPOSITORY/actions/artifacts/$ARTIFACT_ID)"
          test "$(jq -r .name <<<"$payload")" = "$ARTIFACT_NAME"
          test "$(jq -r .workflow_run.id <<<"$payload")" = "$GITHUB_RUN_ID"
          test "$(jq -r .expired <<<"$payload")" = false
          api_digest="$(jq -r .digest <<<"$payload")"
          case "$ARTIFACT_DIGEST" in
            sha256:*) test "$api_digest" = "$ARTIFACT_DIGEST" ;;
            *) test "$api_digest" = "sha256:$ARTIFACT_DIGEST" ;;
          esac
      - name: Download publisher seal
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: ${{ needs.seal.outputs.artifact_name }}
          path: /tmp/published
      - name: Setup Python
        uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
        with:
          version: "0.10.0"
          python-version: "3.12"
      - name: Install
        run: uv sync --extra dev
      - name: Independently reconstruct exact canonical bytes and blobs
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/fresh
          uv run python - <<'PY'
          import hashlib
          import json
          import os
          import subprocess
          from pathlib import Path
          from trade_rl.artifacts.canonical import canonical_json_bytes
          from trade_rl.evaluation.experiments.bootstrap.spot_flow_continuation_prereg import canonical_spot_flow_continuation_protocol, load_spot_flow_continuation_protocol_bytes

          published = Path('/tmp/published/protocol.json').read_bytes()
          seal_index = json.loads(Path('/tmp/published/seal-index.json').read_bytes())
          protocol = canonical_spot_flow_continuation_protocol()
          fresh = protocol.canonical_json_bytes
          expected = os.environ['PROTOCOL_DIGEST']
          if fresh != published:
              raise SystemExit('fresh protocol bytes differ from publisher')
          if hashlib.sha256(fresh).hexdigest() != expected or protocol.digest != expected:
              raise SystemExit('fresh protocol digest differs')
          if load_spot_flow_continuation_protocol_bytes(published).digest != expected:
              raise SystemExit('published strict-loader digest differs')
          paths = [os.environ['SOURCE_PATH'], os.environ['TEST_PATH'], os.environ['HARDENING_TEST_PATH'], os.environ['TARGET_SOURCE_TEST_PATH']]
          blobs = {path: subprocess.check_output(['git','rev-parse',f'HEAD:{path}'], text=True).strip() for path in paths}
          if seal_index.get('blob_shas') != blobs:
              raise SystemExit('source/test blob authority differs')
          Path('/tmp/fresh/fresh-protocol.json').write_bytes(fresh)
          verification = {
              'schema_version': 'issue584_spot_flow_prereg_fresh_v2',
              'issue_number': 584,
              'target_sha': os.environ['TARGET_SHA'],
              'protocol_digest': expected,
              'blob_shas': blobs,
              'protocol_byte_equal': True,
              'source_test_blob_equal': True,
              'fresh_reconstruction_passed': True,
              'economic_values_inspected': False,
              'target_relation_computed': False,
              'training_relation_executed': False,
              'evaluation_pnl_inspected': False,
              'production_eligible': False,
              'live_trading_authorized': False,
          }
          Path('/tmp/fresh/verification.json').write_bytes(canonical_json_bytes(verification))
          print('FRESH_PROTOCOL_BYTE_EQUAL=true')
          print('SOURCE_TEST_BLOB_EQUAL=true')
          print('ECONOMIC_VALUES_INSPECTED=false')
          PY
      - id: meta
        run: echo "name=issue584-spot-flow-prereg-fresh-v2-${GITHUB_RUN_ID}" >> "$GITHUB_OUTPUT"
      - name: Upload fresh reconstruction evidence
        id: upload
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: ${{ steps.meta.outputs.name }}
          path: /tmp/fresh/
          if-no-files-found: error
          compression-level: 0
          retention-days: 90

  audit:
    name: Final preregistration seal v2 audit
    needs: [seal, fresh]
    runs-on: ubuntu-24.04
    steps:
      - name: API-bind both final artifacts
        env:
          GH_TOKEN: ${{ github.token }}
          SEAL_ID: ${{ needs.seal.outputs.artifact_id }}
          SEAL_NAME: ${{ needs.seal.outputs.artifact_name }}
          SEAL_DIGEST: ${{ needs.seal.outputs.artifact_digest }}
          FRESH_ID: ${{ needs.fresh.outputs.artifact_id }}
          FRESH_NAME: ${{ needs.fresh.outputs.artifact_name }}
          FRESH_DIGEST: ${{ needs.fresh.outputs.artifact_digest }}
        shell: bash
        run: |
          set -euo pipefail
          for spec in "$SEAL_ID|$SEAL_NAME|$SEAL_DIGEST" "$FRESH_ID|$FRESH_NAME|$FRESH_DIGEST"; do
            IFS='|' read -r id name output_digest <<<"$spec"
            payload="$(gh api repos/$GITHUB_REPOSITORY/actions/artifacts/$id)"
            test "$(jq -r .name <<<"$payload")" = "$name"
            test "$(jq -r .workflow_run.id <<<"$payload")" = "$GITHUB_RUN_ID"
            test "$(jq -r .expired <<<"$payload")" = false
            api_digest="$(jq -r .digest <<<"$payload")"
            case "$output_digest" in
              sha256:*) test "$api_digest" = "$output_digest" ;;
              *) test "$api_digest" = "sha256:$output_digest" ;;
            esac
          done
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: ${{ needs.seal.outputs.artifact_name }}
          path: /tmp/seal
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: ${{ needs.fresh.outputs.artifact_name }}
          path: /tmp/fresh
      - name: Require immutable byte equality and result-blind boundaries
        shell: bash
        run: |
          set -euo pipefail
          cmp /tmp/seal/protocol.json /tmp/fresh/fresh-protocol.json
          python3 - <<'PY'
          import hashlib
          import json
          import os
          from pathlib import Path
          raw = Path('/tmp/seal/protocol.json').read_bytes()
          if hashlib.sha256(raw).hexdigest() != os.environ['PROTOCOL_DIGEST']:
              raise SystemExit('audited protocol digest differs')
          seal = json.loads(Path('/tmp/seal/seal-index.json').read_bytes())
          fresh = json.loads(Path('/tmp/fresh/verification.json').read_bytes())
          if seal.get('target_sha') != os.environ['TARGET_SHA'] or fresh.get('target_sha') != os.environ['TARGET_SHA']:
              raise SystemExit('audited target differs')
          if seal.get('blob_shas') != fresh.get('blob_shas'):
              raise SystemExit('audited source/test blobs differ')
          if fresh.get('protocol_byte_equal') is not True or fresh.get('source_test_blob_equal') is not True or fresh.get('fresh_reconstruction_passed') is not True:
              raise SystemExit('fresh verification did not pass')
          for payload in (seal, fresh):
              for key in ('economic_values_inspected','target_relation_computed','training_relation_executed','evaluation_pnl_inspected','production_eligible','live_trading_authorized'):
                  if payload.get(key) is not False:
                      raise SystemExit(f'audited forbidden flag differs: {key}')
          print('ISSUE584_PREREG_SEAL_V2_AUDIT=PASS')
          print('PROTOCOL_DIGEST=' + os.environ['PROTOCOL_DIGEST'])
          print('ECONOMIC_VALUES_INSPECTED=false')
          PY
