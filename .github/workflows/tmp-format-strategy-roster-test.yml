name: Temporary format strategy roster RED test

on:
  push:
    branches:
      - refactor/experiment-strategy-roster-authority

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: refactor/experiment-strategy-roster-authority
          fetch-depth: 0

      - name: Set up uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86
        with:
          python-version: "3.12"
          enable-cache: true

      - name: Format test and retire helper
        shell: bash
        run: |
          set -euo pipefail
          uv sync --extra dev
          uv run ruff format tests/architecture/test_experiment_strategy_roster_authority.py
          rm .github/workflows/tmp-format-strategy-roster-test.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tests/architecture/test_experiment_strategy_roster_authority.py .github/workflows/tmp-format-strategy-roster-test.yml
          git diff --cached --check
          git commit -m "test: format experiment strategy roster RED"
          git push origin HEAD:refactor/experiment-strategy-roster-authority
