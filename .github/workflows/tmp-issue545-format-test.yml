name: Temporary Issue 545 format test

on:
  push:
    branches:
      - verify/issue545-prereg-red
    paths:
      - .github/workflows/tmp-issue545-format-test.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: research/issue545-economic-action-prereg
      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
        with:
          version: "0.10.0"
          python-version: "3.12"
      - run: uv sync --extra dev
      - name: Format only the prereg test
        run: uv run ruff format tests/evaluation/experiments/bootstrap/test_mean_reversion_economic_gate_prereg.py
      - name: Commit formatter-only change
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          test "$(git diff --name-only)" = "tests/evaluation/experiments/bootstrap/test_mean_reversion_economic_gate_prereg.py"
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add tests/evaluation/experiments/bootstrap/test_mean_reversion_economic_gate_prereg.py
          git commit -m 'style: format issue545 prereg test'
          git push origin HEAD:research/issue545-economic-action-prereg
