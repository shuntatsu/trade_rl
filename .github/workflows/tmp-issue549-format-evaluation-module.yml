name: Temporary Issue 549 format evaluation module

on:
  push:
    branches:
      - verify/issue549-format-evaluation-module

permissions:
  contents: write

env:
  TARGET_SHA: "8edaab169001437887bfa0a9311dd58936461a8e"
  TARGET_BRANCH: research/issue549-economic-gate-evaluation

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ env.TARGET_SHA }}
      - run: test "$(git rev-parse HEAD)" = "$TARGET_SHA"
      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
        with:
          version: "0.10.0"
          python-version: "3.12"
      - run: uv sync --extra dev
      - run: uv run ruff format trade_rl/evaluation/experiments/bootstrap/mean_reversion_economic_gate_evaluation.py
      - name: Commit formatter-only change
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add trade_rl/evaluation/experiments/bootstrap/mean_reversion_economic_gate_evaluation.py
          git diff --cached --quiet && exit 0
          git commit -m "style: format issue549 evaluation module"
          git push origin "HEAD:${TARGET_BRANCH}"
