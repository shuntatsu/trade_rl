name: Temporary Issue 578 preregistration RED v2

on:
  push:
    branches:
      - verify/issue578-prereg-red-v2

permissions:
  contents: read

env:
  TARGET_SHA: 06d3815695fa42de8a30468c7e915169a6dd288a

jobs:
  red:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout exact RED head
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ env.TARGET_SHA }}
          persist-credentials: false
      - name: Assert exact target
        run: test "$(git rev-parse HEAD)" = "$TARGET_SHA"
      - name: Setup uv
        uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
        with:
          version: "0.10.0"
          python-version: "3.12"
      - run: uv sync --extra dev
      - name: Test source quality
        run: |
          uv run ruff format --check tests/evaluation/experiments/bootstrap/test_spot_aggtrades_source_prereg.py
          uv run ruff check tests/evaluation/experiments/bootstrap/test_spot_aggtrades_source_prereg.py
      - name: Require missing-production RED
        shell: bash
        run: |
          set +e
          uv run pytest -q tests/evaluation/experiments/bootstrap/test_spot_aggtrades_source_prereg.py > /tmp/red.log 2>&1
          status=$?
          set -e
          cat /tmp/red.log
          test "$status" -ne 0
          grep -Eq "ModuleNotFoundError|No module named.*spot_aggtrades_source_prereg" /tmp/red.log
          ! grep -q "SyntaxError" /tmp/red.log
