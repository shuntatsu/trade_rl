name: Temporary Markdown Guide docs promotion

on:
  push:
    branches:
      - guide/markdown-first-redesign
    paths:
      - .github/workflows/tmp-promote-markdown-guide-docs.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Checkout implementation branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: guide/markdown-first-redesign
          fetch-depth: 1
          persist-credentials: true

      - name: Promote durable Guide docs
        run: python3 .github/tmp_patch_markdown_guide_docs.py

      - name: Commit durable docs only
        shell: bash
        run: |
          set -euo pipefail
          git add docs/AGENTS.md docs/README.md
          git diff --cached --quiet && exit 0
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "docs: promote Markdown-first Guide contract"
          git push origin HEAD:guide/markdown-first-redesign
