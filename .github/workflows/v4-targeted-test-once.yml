name: V4 Ruff format once

on:
  push:
    branches:
      - design/causal-alpha-v4-hierarchical-teacher

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact head
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: ${{ github.sha }}
          persist-credentials: true
      - name: Set up uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86
        with:
          python-version: '3.12'
          enable-cache: true
      - name: Install formatter
        run: uv sync --extra dev
      - name: Apply bounded Ruff fixes
        run: |
          files=(
            scripts/run_universal_causal_alpha_v4_research.py
            tests/workflows/test_universal_causal_alpha_v4_admission.py
            tests/workflows/test_universal_causal_alpha_v4_selection.py
            tests/workflows/test_universal_causal_alpha_v4_stage_helpers.py
            trade_rl/workflows/universal_causal_alpha_v4_admission.py
            trade_rl/workflows/universal_causal_alpha_v4_pipeline.py
            trade_rl/workflows/universal_causal_alpha_v4_runner.py
            trade_rl/workflows/universal_causal_alpha_v4_stage_execution.py
            trade_rl/workflows/universal_causal_alpha_v4_selection.py
          )
          uv run ruff check --fix "${files[@]}"
          uv run ruff format "${files[@]}"
          uv run ruff check "${files[@]}"
          uv run ruff format --check "${files[@]}"
          git diff --check
      - name: Commit formatter output
        env:
          BRANCH_NAME: ${{ github.ref_name }}
        run: |
          files=(
            scripts/run_universal_causal_alpha_v4_research.py
            tests/workflows/test_universal_causal_alpha_v4_admission.py
            tests/workflows/test_universal_causal_alpha_v4_selection.py
            tests/workflows/test_universal_causal_alpha_v4_stage_helpers.py
            trade_rl/workflows/universal_causal_alpha_v4_admission.py
            trade_rl/workflows/universal_causal_alpha_v4_pipeline.py
            trade_rl/workflows/universal_causal_alpha_v4_runner.py
            trade_rl/workflows/universal_causal_alpha_v4_stage_execution.py
            trade_rl/workflows/universal_causal_alpha_v4_selection.py
          )
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -- "${files[@]}"
          if git diff --cached --quiet; then
            echo 'No formatter changes were required.'
            exit 0
          fi
          git commit -m 'style: apply ruff to causal alpha v4'
          git push origin "HEAD:${BRANCH_NAME}"
