{
  "operations": [
    {
      "kind": "create",
      "path": "trade_rl/catalog/postgres_connection.py",
      "content": "\"\"\"Public optional PostgreSQL connection construction for catalog adapters.\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Any\n\n\ndef import_psycopg() -> Any | None:\n    try:\n        import psycopg\n    except ImportError:\n        return None\n    return psycopg\n\n\ndef default_connection_factory(database_url: str) -> Any:\n    psycopg = import_psycopg()\n    if psycopg is None:\n        raise RuntimeError(\n            \"PostgreSQL catalog support requires the 'postgres' optional dependency\"\n        )\n    return psycopg.connect(database_url)\n\n\n__all__ = [\"default_connection_factory\", \"import_psycopg\"]\n"
    },
    {
      "kind": "replace",
      "path": "trade_rl/catalog/postgres.py",
      "old": "from trade_rl.catalog.migrations import apply_migrations\n",
      "new": "from trade_rl.catalog.migrations import apply_migrations\nfrom trade_rl.catalog.postgres_connection import default_connection_factory\n"
    },
    {
      "kind": "replace",
      "path": "trade_rl/catalog/postgres.py",
      "old": "def _import_psycopg() -> Any | None:\n    try:\n        import psycopg\n    except ImportError:\n        return None\n    return psycopg\n\n\ndef _default_connection_factory(database_url: str) -> Any:\n    psycopg = _import_psycopg()\n    if psycopg is None:\n        raise RuntimeError(\n            \"PostgreSQL catalog support requires the 'postgres' optional dependency\"\n        )\n    return psycopg.connect(database_url)\n\n\n",
      "new": ""
    },
    {
      "kind": "replace",
      "path": "trade_rl/catalog/postgres.py",
      "old": "        self._connection_factory = connection_factory or _default_connection_factory\n",
      "new": "        self._connection_factory = connection_factory or default_connection_factory\n"
    },
    {
      "kind": "replace",
      "path": "trade_rl/catalog/postgres_sealed_test.py",
      "old": "from trade_rl.catalog.postgres import _default_connection_factory\n",
      "new": "from trade_rl.catalog.postgres_connection import default_connection_factory\n"
    },
    {
      "kind": "replace",
      "path": "trade_rl/catalog/postgres_sealed_test.py",
      "old": "        self._connection_factory = connection_factory or _default_connection_factory\n",
      "new": "        self._connection_factory = connection_factory or default_connection_factory\n"
    }
  ]
}
