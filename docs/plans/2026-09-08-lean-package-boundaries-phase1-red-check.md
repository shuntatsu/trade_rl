# Phase 1 RED verification head

The next action is to open the implementation branch as a Draft PR against `main` so repository CI can execute this intentionally failing architecture contract. The first observed CI failure must be inspected before any production migration is committed.
